for n in range (2,11):
    print(("Creating VLAN") + str(n))

# this demonstrates how to loop through a range, then concatenate the converted n to the text
#output should be "Creating VLAN 2" followed by "Creating VLAN 3" and so on until the max of VLAN 10 (up to 11, not inclusive)
